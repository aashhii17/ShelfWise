# TODO - Push project to Git

- [ ] Check if .git exists; if not, initialize git.
- [ ] Add .gitignore (if missing) and clean up generated files.
- [ ] Create initial commit.
- [ ] Create remote GitHub repo (or use existing) and add as origin.
- [ ] Push main branch to remote.
- [ ] Provide final commands the user can run.

